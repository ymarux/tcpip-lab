# 作業ルール

## 目的と正本

- TCP/IPの仕組みを、個別の実装と実験を通して理解する。
- README.mdを全体方針、各labs配下の文書をテーマごとの仕様・計画とする。
- tcpip-lab_handoff.mdは方針の出発点として保存する。
- 最初から統合TCP/IPスタックやproduction-readyな製品を目指さない。

## ブランチ運用

- 1テーマにつき1チャット・1作業ブランチを基本とする。
- テーマ開始時にmainから lab/<テーマ名> を作成して切り替える。テーマ名はlabs/配下のディレクトリ名と揃える（例：labs/arp/ → lab/arp）。
- 同じテーマの作業を再開するときは既存のブランチを使う。作成・切り替え前に現在のブランチと未コミット変更を確認し、別テーマの変更を混在させない。
- テーマのコード・仕様・実験ノート・最終レポートは同じブランチで管理する。意味のある作業単位でコミットする。
- 必要な検証を終え、spec/report.mdとテーマのREADME.mdを整えてからmainへの統合対象とする。
- リポジトリ全体の方針整備はテーマの変更と分けてコミットする。

## ユーザーとCodexの役割

- 問いの設定、検証の実施、結果の判断はユーザーが主導する。
- Codexは仕様の説明、依頼されたコードの作成・修正、実験手順の整理、結果の読み解きと文書化をフォローする。
- ユーザーの依頼なく問い・到達点を決定したり、実装・検証を先へ進めたりしない。
- チャットで説明したテーマの内容は、対応する資料にも反映する。仕様の説明とユーザーが実測した検証結果を区別する。
- notes.mdにはユーザーの検証と考察を記録し、report.mdはその記録を根拠にまとめる。

## 実装

- 対象はLinux、基本言語はC。初期の言語標準はC11。
- 3rd partyライブラリは原則使用しない。学習対象プロトコルの生成・解析・処理をライブラリに委譲しない。
- socket、ioctl、sendto、recvfrom等のOS境界API、およびメモリ操作・表示・アドレス表記変換等の補助処理は利用してよい。
- Ethernet/ARPヘッダのフィールド配置・生成・解析は自作する。
- フレームのバイト列を構造体へ直接キャストせず、長さを検証して明示的に読み書きする。byte orderをコード上で示す。
- 小さな実装単位で進める。テーマ横断の共通ライブラリや汎用フレームワークを先回りして作らない。
- コメントは処理の理由やプロトコルとの対応を説明する。文書・作業報告は日本語を基本とする。

## 検証と記録

- 1テーマにつき1チャットで進め、小さな問いごとに実装・実験・考察を繰り返す。後から辿れるよう、決定事項と結果はファイルにも残す。
- 各テーマは labs/<テーマ名>/ に置き、直下のREADME.mdを入口にする。コード・Makefile・実行スクリプトはsrc/、文書はspec/に分ける。README.mdからspec/spec.md（仕様）、spec/notes.md（実験ノート）、spec/report.md（最終検証レポート）へ相対リンクを張る。
- 最終出力は各テーマのspec/report.mdに統一する。途中経過はnotes.mdに蓄積し、テーマ完了時に根拠とともにreport.mdへ整理する。
- report.mdは「目的・検証したい問い」「仕組み・必要な仕様」「検証環境・方法」「実装の要点」「検証結果」「考察・分かったこと」「制約・残課題」「参考資料」で構成する。未検証の内容を結果として書かない。
- 実装前に到達点、仕様、対象外、検証方法を文書化する。
- 初期の警告オプションは -Wall -Wextra -Wpedantic。コンパイラとバージョンを実験結果に記録する。
- バイト列の検証は権限不要で実行できるようにし、Linuxでの送受信実験と分ける。
- tcpdump / Wiresharkは独立した観測・照合に利用できるが、自作する処理の代替にはしない。
- ネットワーク実験は専用Network Namespaceとvethを基本とする。作成物を識別できる名前にし、既存環境と衝突したら上書きせず停止する。
- 実行した検証と未実行の検証を区別する。実験条件・コマンド・予想・結果・制約を記録する。
- 実装や計画を変更したら関連文書も更新する。
